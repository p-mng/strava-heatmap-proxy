version: "2"
linters:
  # https://golangci-lint.run/usage/linters/#enabled-by-default
  enable:
    - exhaustive
    - exhaustruct
    - goconst
    - gocritic
    - nilnil
    - revive
    - unused
  exclusions:
    presets:
      - comments
  settings:
    exhaustruct:
      allow-empty: true
